window.toast = (message) => {
    if (!message) return;
    Toastify({
        text: message,
        gravity: "bottom",
        position: "right",
        style: {
            background: "#333",
        },
    }).showToast();
}